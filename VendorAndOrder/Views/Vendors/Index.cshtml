@{
  Layout = "_Layout";
}


@using System;
@using VendorAndOrder.Models;
@if (@Model.Count == 0)
{
  <h1>No vendors found</h1>
  <p>Please use the menu at the top to add a vendor.</p>
}
else
{
  @foreach (Vendor vendor in Model)
  {
    <h1>@vendor.VendorName</h1>
    <p>@vendor.VendorDescription.</p>
  }
}